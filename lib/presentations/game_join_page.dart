import 'package:flutter/material.dart';

class JoinGamePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('게임 조인하기')),
      body: Center(
        // Draft 상태인 게임 목록을 ListView 또는 유사한 위젯으로 표시
        // 각 게임에 대한 상세 정보 및 매칭 버튼 추가
      ),
    );
  }
}
